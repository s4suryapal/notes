package com.notesai.easynotes.ai.smart.notepad.ocr.docscanner.privatenotes


object Utility {
    private const val TAG = "Utility"

    fun trackEvent(name: String) {
        // Placeholder: Integrate with analytics if desired; currently logs only
        try {
        } catch (_: Exception) {}
    }
}
